Raspberry pi touchscreen ACHT Application (Qt Quick)

- Two wire test is completely implemented (To use simulated PDF tick simulate, give END and ABCDEA command).

- Iso/Insu tests are builded upto packet creation (making nets) after logic is confused, and we are stopping at buildNetData() function even though entire implementation done upto pdf there is problem in commands sending and receiving with ICD it will easy to use existing architecture. Problem is this new architecture sending single packet of data we need to send whole net data and wait for end after send test command and wait for end after that you get result for Isolation it is ABCDEB for insulation ABCDEC.

IMPROVEMENTS
- Layout structure (simulate checkbox row).

- On back button port disconnect (no dialog required).

- msgBox (make text small or use word wrap with scroll).

- no qDebug() only login->writeToNotes().

- Load time before pdf generation (loading bar or progress bar without UI hand even when file upload also).

- Iso/Insu Net packet + pdf generation.

Ver1.0 ----------------------------------------------------------------------------------------------------------------
- Taking this Raspberry pi CHT as base project for building the new SolarACHT_2 application.